ViewRSS
=======

RSS feed reader with web page visualization.

Installation
============

Install first node-webkit.
Clone this repository. Move to the directory and then launch:

```bash
$ nw .
```

Objectives
==========

* Build the front-end with angularJS + node-webkit
* Build the back-end with golang
* Fetch rss feeds and show them on the UI